# LS-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public lab-science adapter slice with a minimal regulated experiment and evidence scenario bundle.

## Acceptance Highlights

- `adapter-lab-science` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/lab-science-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
