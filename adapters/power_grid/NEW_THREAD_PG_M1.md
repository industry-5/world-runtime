# PG-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public power-grid adapter slice with a minimal scenario bundle for balancing and contingency response.

## Acceptance Highlights

- `adapter-power-grid` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/power-grid-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
