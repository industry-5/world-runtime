# SS-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public semantic-system adapter slice with a minimal scenario bundle that proves semantic coherence and governed meaning-change boundaries.

## Acceptance Highlights

- `adapter-semantic-system` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/semantic-system-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
