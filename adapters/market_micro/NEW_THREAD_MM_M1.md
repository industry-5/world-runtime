# MM-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public market-micro adapter slice with a minimal risk-limit and exposure scenario bundle.

## Acceptance Highlights

- `adapter-market-micro` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/market-micro-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
