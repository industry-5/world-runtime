# MA-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public multi-agent AI adapter slice with a minimal proposal, conflict, and review scenario bundle.

## Acceptance Highlights

- `adapter-multi-agent-ai` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/multi-agent-ai-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
