# MPG-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public multiplayer-game adapter slice with a minimal shared-state concurrency scenario bundle.

## Acceptance Highlights

- `adapter-multiplayer-game` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/multiplayer-game-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
