# AV-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public autonomous-vehicle adapter slice with a minimal motion-planning and intervention scenario bundle.

## Acceptance Highlights

- `adapter-autonomous-vehicle` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/autonomous-vehicle-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
