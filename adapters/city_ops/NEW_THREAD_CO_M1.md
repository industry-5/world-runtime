# CO-M1 - Adapter Contract, Schemas, Minimal Scenario, Registry Wiring

## Objective

Create the first public city-ops adapter slice with a minimal cross-agency incident scenario bundle.

## Acceptance Highlights

- `adapter-city-ops` exists on the shared adapter contract
- package-local schemas and default policy exist
- `examples/scenarios/city-ops-mini/` exists with the standard public scenario-bundle shape
- registry/example/test surfaces recognize the new package
