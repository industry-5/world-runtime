{
  "policy_id": "policy.__ADAPTER_SLUG__.default",
  "policy_name": "__ADAPTER_NAME__ default policy",
  "policy_type": "validation",
  "description": "Starter policy pack for __ADAPTER_NAME__.",
  "scope": {
    "adapters": ["__ADAPTER_ID__"],
    "action_types": ["__ADAPTER_SLUG__.action.apply"]
  },
  "rules": [
    {
      "rule_id": "rule.__ADAPTER_SLUG__.requires_reason",
      "rule_name": "Action requires reason",
      "description": "Require a non-empty reason on outbound actions.",
      "condition": {
        "field": "proposed_action.parameters.reason",
        "operator": "equals",
        "value": ""
      },
      "outcome": "deny",
      "severity": "high",
      "message_template": "Reason is required for __ADAPTER_NAME__ actions."
    }
  ],
  "default_outcome": "allow",
  "priority": 100,
  "enabled": true,
  "created_at": "2026-03-09T00:00:00Z",
  "version": 1
}
