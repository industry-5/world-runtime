# city-ops-mini

- Public adapter: `adapter-city-ops`.
- Premise: coordinate a flooded transit-and-water incident with unified command, service continuity, and approval-visible civic tradeoffs.
- Standard bundle: this directory carries the shared non-overlay public scenario-bundle baseline.
- Supplemental proof: `coordination_options.json` preserves cross-agency alternatives beside the baseline bundle.
- Overlay host note: `examples/scenarios/digital-twin-mini/host_bindings.json` binds the public digital-twin overlay here as its second host proof.
