# air-traffic-mini

- Public adapter: `adapter-air-traffic`.
- Premise: resolve a weather-driven arrival conflict under explicit safety and approval pressure.
- Standard bundle: this directory carries the shared non-overlay public scenario-bundle baseline.
- Supplemental proof: `conflicting_proposals.json` preserves the richer constrained-alternative review path on top of the shared baseline.
