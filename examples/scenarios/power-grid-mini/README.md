# power-grid-mini

- Public adapter: `adapter-power-grid`.
- Premise: stabilize a heat-wave transmission contingency with explicit simulation evidence and least-bad-choice governance.
- Standard bundle: this directory carries the shared non-overlay public scenario-bundle baseline.
- Supplemental proof: `contingency_options.json` preserves contingency-response alternatives beside the baseline bundle.
- Overlay host note: `examples/scenarios/digital-twin-mini/host_bindings.json` binds the public digital-twin overlay here as its first host proof.
