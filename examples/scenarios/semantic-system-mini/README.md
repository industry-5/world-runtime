# semantic-system-mini

- Public adapter: `adapter-semantic-system`.
- Premise: govern a breaking meaning change without losing provenance or relationship coherence.
- Standard bundle: this directory carries the shared non-overlay public scenario-bundle baseline.
- Supplemental proof: `conflicting_proposals.json` preserves semantic-change alternatives beside the baseline bundle.
