# multiplayer-game-mini

- Public adapter: `adapter-multiplayer-game`.
- Premise: govern contested shared-state reconciliation inside a live match shard under simultaneous intent pressure.
- Standard bundle: this directory carries the shared non-overlay public scenario-bundle baseline.
- Supplemental proof: `resolution_options.json` preserves concurrency-resolution alternatives beside the baseline bundle.
